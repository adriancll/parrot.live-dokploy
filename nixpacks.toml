# Fixed nixpacks.toml for parrot.live
[phases.setup]
nixPkgs = ['nodejs', 'npm']

[phases.install]
cmds = ['npm ci --include=dev']

[phases.build]
cmds = []

[phases.run]
cmd = 'PARROT_PORT=${PORT:-3000} node index.js'
